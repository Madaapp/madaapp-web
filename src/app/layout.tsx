import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";
import { cn } from "@/lib/utils";

const inter = Inter({ subsets: ["latin"] });

// const APP_NAME = "Mada App";
// const APP_DEFAULT_TITLE = "Mada App";
// const APP_TITLE_TEMPLATE = "Mada App";
// const APP_DESCRIPTION = "Mada App";

export const metadata: Metadata = {
  title: "Create Next App",
  description: "Generated by create next app",
  manifest: "/manifest.json",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={cn(
          inter.className,
          "flex bg-slate-400 flex-col items-center"
        )}
      >
        <main className="w-full  h-screen  max-w-screen-md">
          <div className="bg-slate-200 h-full py-10 px-4 flex flex-col relative items-center">
            {children}
          </div>
        </main>
      </body>
    </html>
  );
}
